# Prevent browser from caching the admin SPA entry — ensures users always
# get the latest index.html rather than a stale cached version.
<FilesMatch "\.html$">
  Header set Cache-Control "no-store, no-cache, must-revalidate"
  Header set Pragma "no-cache"
  Header set Expires "0"
</FilesMatch>
